/**
 * @module @projective/ui/utils
 * Framework-agnostic helpers the UI layer leans on — string modifiers, math (slider/log/angle
 * conversions), date/time math, colour utilities (tag-theme generation) and markdown/validation
 * pipes. Pure functions only; no DOM, no data layer — safe to lift into any project.
 */
export * from '@projective/utils';
