/**
 * @module @projective/ui/charts/finance
 * The D3 finance-chart entry (runway, pipeline, forecast). Split out from `@projective/ui/charts`
 * because it pulls the heavier d3 dependency graph — import it only where finance viz is rendered.
 */
export * from '@projective/charts/finance';
