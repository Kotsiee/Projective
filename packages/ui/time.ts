/**
 * @module @projective/ui/time
 * Timezone-aware scheduling surfaces — the boundless-velocity calendar viewport and the Schedule
 * Minimap accelerator scrollbar. All calendar / time-picker / scheduling configuration consolidates
 * here (moved out of `@projective/ui/fields`), so anything that reasons about dates and schedules
 * lives under a single namespace.
 */
export * from '@projective/time';
