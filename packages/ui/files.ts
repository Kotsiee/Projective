/**
 * @module @projective/ui/files
 * The shared file-library surfaces — the UploadFile multi-column browser, HandoverLibrary, file
 * grid / details / directory-tree and the library store. Any feature that needs to pick, preview or
 * bundle files reuses these instead of re-implementing a picker.
 */
export * from '@projective/files';
