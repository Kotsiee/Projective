import WalletSettingsIsland from '@features/dashboard/wallet/islands/WalletSettings.island.tsx';

/** `/wallet/settings` — wallet preferences and connected bank cards. */
export default function WalletSettingsPage() {
	return <WalletSettingsIsland />;
}
