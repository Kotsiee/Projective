import WalletHubIsland from '@features/dashboard/wallet/islands/WalletHub.island.tsx';

/** `/wallet` — the primary Financial Overview dashboard. */
export default function WalletOverviewPage() {
	return <WalletHubIsland />;
}
