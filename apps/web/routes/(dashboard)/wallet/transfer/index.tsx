import WalletTransferIsland from '@features/dashboard/wallet/islands/WalletTransfer.island.tsx';

/** `/wallet/transfer` — payouts, withdrawals and top-up settings. */
export default function WalletTransferPage() {
	return <WalletTransferIsland />;
}
