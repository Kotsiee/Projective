import WalletLedgerIsland from '@features/dashboard/wallet/islands/WalletLedger.island.tsx';

/** `/wallet/ledger` — the dedicated full-page in-depth transactions ledger. */
export default function WalletLedgerPage() {
	return <WalletLedgerIsland />;
}
