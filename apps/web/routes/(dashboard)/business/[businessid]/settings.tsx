import BusinessSettings from '@features/dashboard/business/islands/BusinessSettings.island.tsx';

/**
 * Business Profile Management settings (US-008 AC2). Renders the settings island, which manages the
 * active business's legal/display name, billing email and administrative logo.
 */
export default function BusinessSettingsRoute() {
	return <BusinessSettings />;
}
