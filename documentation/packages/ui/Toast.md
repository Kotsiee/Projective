# UI Component: Toast

> **DEPRECATED / NEEDS CONTENT:** This file previously contained a byte-for-byte copy-paste of
> `Stepper.md` (wrong component entirely — no Toast-specific content ever existed here). The
> duplicate content has been removed rather than left in place, since it would actively mislead an
> agent into documenting the wrong component's API.
>
> No accurate documentation of the `Toast` component currently exists in `documentation/packages/`
> or in `codebase_context.md`. If you are implementing or touching the Toast component, please
> author real documentation here (props, variants, dismiss/timeout behavior, stacking behavior)
> rather than restoring the old placeholder.
